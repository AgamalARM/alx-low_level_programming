#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print num frm 0 to 9
 *
 * Return: 0 for success
*/

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
